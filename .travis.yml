sudo: false
language: node_js
node_js:
  - 'node'
  - 'lts/*'
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
  local-dir: dist
